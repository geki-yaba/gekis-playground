    unsigned int i = 0;
    switch(iset[i++])
    {
        case 'a':
            switch(iset[i++])
            {
            case 'd':
                switch(iset[i++])
                {
                case 'd':
                    switch(iset[i++])
                    {
                    case 'p':
                        switch(iset[i++])
                        {
                        case 'd': /* addpd */
                            if (iset[i] == '\0') { ++data.sse2; }
                            break;
                        case 's': /* addps */
                            if (iset[i] == '\0') { ++data.sse; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 's':
                        switch(iset[i++])
                        {
                        case 'd': /* addsd */
                            if (iset[i] == '\0') { ++data.sse2; }
                            break;
                        case 's': /* addss */
                            if (iset[i] == '\0') { ++data.sse; }
                            break;
                        case 'u':
                            switch(iset[i++])
                            {
                            case 'b':
                                switch(iset[i++])
                                {
                                case 'p':
                                    switch(iset[i++])
                                    {
                                    case 'd': /* addsubpd */
                                        if (iset[i] == '\0') { ++data.sse3; }
                                        break;
                                    case 's': /* addsubps */
                                        if (iset[i] == '\0') { ++data.sse3; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'n':
                switch(iset[i++])
                {
                case 'd':
                    switch(iset[i++])
                    {
                    case 'n':
                        switch(iset[i++])
                        {
                        case 'p':
                            switch(iset[i++])
                            {
                            case 'd': /* andnpd */
                                if (iset[i] == '\0') { ++data.sse2; }
                                break;
                            case 's': /* andnps */
                                if (iset[i] == '\0') { ++data.sse; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 'p':
                        switch(iset[i++])
                        {
                        case 'd': /* andpd */
                            if (iset[i] == '\0') { ++data.sse2; }
                            break;
                        case 's': /* andps */
                            if (iset[i] == '\0') { ++data.sse; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            default:
                ++data.unused;
                break;
            }
            break;
        case 'b':
            switch(iset[i++])
            {
            case 'l':
                switch(iset[i++])
                {
                case 'e':
                    switch(iset[i++])
                    {
                    case 'n':
                        switch(iset[i++])
                        {
                        case 'd':
                            switch(iset[i++])
                            {
                            case 'p':
                                switch(iset[i++])
                                {
                                case 'd': /* blendpd */
                                    if (iset[i] == '\0') { ++data.sse4_1; }
                                    break;
                                case 's': /* blendps */
                                    if (iset[i] == '\0') { ++data.sse4_1; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            case 'v':
                                switch(iset[i++])
                                {
                                case 'p':
                                    switch(iset[i++])
                                    {
                                    case 'd': /* blendvpd */
                                        if (iset[i] == '\0') { ++data.sse4_1; }
                                        break;
                                    case 's': /* blendvps */
                                        if (iset[i] == '\0') { ++data.sse4_1; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 's':
                switch(iset[i++])
                {
                case 'w':
                    switch(iset[i++])
                    {
                    case 'a':
                        switch(iset[i++])
                        {
                        case 'p': /* bswap */
                            if (iset[i] == '\0') { ++data.i486; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            default:
                ++data.unused;
                break;
            }
            break;
        case 'c':
            switch(iset[i++])
            {
            case 'l':
                switch(iset[i++])
                {
                case 'f':
                    switch(iset[i++])
                    {
                    case 'l':
                        switch(iset[i++])
                        {
                        case 'u':
                            switch(iset[i++])
                            {
                            case 's':
                                switch(iset[i++])
                                {
                                case 'h': /* clflush */
                                    if (iset[i] == '\0') { ++data.sse2; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'm':
                switch(iset[i++])
                {
                case 'o':
                    switch(iset[i++])
                    {
                    case 'v':
                        switch(iset[i++])
                        {
                        case 'c':
                            switch(iset[i++])
                            {
                            case 'c': /* cmovcc */
                                if (iset[i] == '\0') { ++data.ppro; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'p':
                    switch(iset[i++])
                    {
                    case 'p':
                        switch(iset[i++])
                        {
                        case 'd': /* cmppd */
                            if (iset[i] == '\0') { ++data.sse2; }
                            break;
                        case 's': /* cmpps */
                            if (iset[i] == '\0') { ++data.sse; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 's':
                        switch(iset[i++])
                        {
                        case 'd': /* cmpsd */
                            if (iset[i] == '\0') { ++data.sse2; }
                            break;
                        case 's': /* cmpss */
                            if (iset[i] == '\0') { ++data.sse; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 'x':
                        switch(iset[i++])
                        {
                        case 'c':
                            switch(iset[i++])
                            {
                            case 'h':
                                switch(iset[i++])
                                {
                                case '8':
                                    switch(iset[i++])
                                    {
                                    case 'b': /* cmpxch8b */
                                        if (iset[i] == '\0') { ++data.i586; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                case 'g': /* cmpxchg */
                                    if (iset[i] == '\0') { ++data.i486; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'o':
                switch(iset[i++])
                {
                case 'm':
                    switch(iset[i++])
                    {
                    case 'i':
                        switch(iset[i++])
                        {
                        case 's':
                            switch(iset[i++])
                            {
                            case 'd': /* comisd */
                                if (iset[i] == '\0') { ++data.sse2; }
                                break;
                            case 's': /* comiss */
                                if (iset[i] == '\0') { ++data.sse; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'p':
                switch(iset[i++])
                {
                case 'u':
                    switch(iset[i++])
                    {
                    case 'i':
                        switch(iset[i++])
                        {
                        case 'd': /* cpuid */
                            if (iset[i] == '\0') { ++data.cpuid;++data.i586; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'r':
                switch(iset[i++])
                {
                case 'c':
                    switch(iset[i++])
                    {
                    case '3':
                        switch(iset[i++])
                        {
                        case '2': /* crc32 */
                            if (iset[i] == '\0') { ++data.sse4_2; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'v':
                switch(iset[i++])
                {
                case 't':
                    switch(iset[i++])
                    {
                    case 'd':
                        switch(iset[i++])
                        {
                        case 'q':
                            switch(iset[i++])
                            {
                            case '2':
                                switch(iset[i++])
                                {
                                case 'p':
                                    switch(iset[i++])
                                    {
                                    case 'd': /* cvtdq2pd */
                                        if (iset[i] == '\0') { ++data.sse2; }
                                        break;
                                    case 's': /* cvtdq2ps */
                                        if (iset[i] == '\0') { ++data.sse2; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 'p':
                        switch(iset[i++])
                        {
                        case 'd':
                            switch(iset[i++])
                            {
                            case '2':
                                switch(iset[i++])
                                {
                                case 'p':
                                    switch(iset[i++])
                                    {
                                    case 'i': /* cvtpd2pi */
                                        if (iset[i] == '\0') { ++data.sse2; }
                                        break;
                                    case 'q': /* cvtpd2pq */
                                        if (iset[i] == '\0') { ++data.sse2; }
                                        break;
                                    case 's': /* cvtpd2ps */
                                        if (iset[i] == '\0') { ++data.sse2; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 'i':
                            switch(iset[i++])
                            {
                            case '2':
                                switch(iset[i++])
                                {
                                case 'p':
                                    switch(iset[i++])
                                    {
                                    case 'd': /* cvtpi2pd */
                                        if (iset[i] == '\0') { ++data.sse2; }
                                        break;
                                    case 's': /* cvtpi2ps */
                                        if (iset[i] == '\0') { ++data.sse; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 's':
                            switch(iset[i++])
                            {
                            case '2':
                                switch(iset[i++])
                                {
                                case 'd':
                                    switch(iset[i++])
                                    {
                                    case 'q': /* cvtps2dq */
                                        if (iset[i] == '\0') { ++data.sse2; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                case 'p':
                                    switch(iset[i++])
                                    {
                                    case 'd': /* cvtps2pd */
                                        if (iset[i] == '\0') { ++data.sse2; }
                                        break;
                                    case 'i': /* cvtps2pi */
                                        if (iset[i] == '\0') { ++data.sse; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 's':
                        switch(iset[i++])
                        {
                        case 'd':
                            switch(iset[i++])
                            {
                            case '2':
                                switch(iset[i++])
                                {
                                case 's':
                                    switch(iset[i++])
                                    {
                                    case 'i': /* cvtsd2si */
                                        if (iset[i] == '\0') { ++data.sse2; }
                                        break;
                                    case 's': /* cvtsd2ss */
                                        if (iset[i] == '\0') { ++data.sse2; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 'i':
                            switch(iset[i++])
                            {
                            case '2':
                                switch(iset[i++])
                                {
                                case 's':
                                    switch(iset[i++])
                                    {
                                    case 'd': /* cvtsi2sd */
                                        if (iset[i] == '\0') { ++data.sse2; }
                                        break;
                                    case 's': /* cvtsi2ss */
                                        if (iset[i] == '\0') { ++data.sse; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 's':
                            switch(iset[i++])
                            {
                            case '2':
                                switch(iset[i++])
                                {
                                case 's':
                                    switch(iset[i++])
                                    {
                                    case 'd': /* cvtss2sd */
                                        if (iset[i] == '\0') { ++data.sse2; }
                                        break;
                                    case 'i': /* cvtss2si */
                                        if (iset[i] == '\0') { ++data.sse; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 't':
                        switch(iset[i++])
                        {
                        case 'p':
                            switch(iset[i++])
                            {
                            case 'd':
                                switch(iset[i++])
                                {
                                case '2':
                                    switch(iset[i++])
                                    {
                                    case 'd':
                                        switch(iset[i++])
                                        {
                                        case 'q': /* cvttpd2dq */
                                            if (iset[i] == '\0') { ++data.sse2; }
                                            break;
                                        default:
                                            ++data.unused;
                                            break;
                                        }
                                        break;
                                    case 'p':
                                        switch(iset[i++])
                                        {
                                        case 'i': /* cvttpd2pi */
                                            if (iset[i] == '\0') { ++data.sse2; }
                                            break;
                                        default:
                                            ++data.unused;
                                            break;
                                        }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            case 's':
                                switch(iset[i++])
                                {
                                case '2':
                                    switch(iset[i++])
                                    {
                                    case 'd':
                                        switch(iset[i++])
                                        {
                                        case 'q': /* cvttps2dq */
                                            if (iset[i] == '\0') { ++data.sse2; }
                                            break;
                                        default:
                                            ++data.unused;
                                            break;
                                        }
                                        break;
                                    case 'p':
                                        switch(iset[i++])
                                        {
                                        case 'i': /* cvttps2pi */
                                            if (iset[i] == '\0') { ++data.sse; }
                                            break;
                                        default:
                                            ++data.unused;
                                            break;
                                        }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 's':
                            switch(iset[i++])
                            {
                            case 'd':
                                switch(iset[i++])
                                {
                                case '2':
                                    switch(iset[i++])
                                    {
                                    case 's':
                                        switch(iset[i++])
                                        {
                                        case 'i': /* cvttsd2si */
                                            if (iset[i] == '\0') { ++data.sse2; }
                                            break;
                                        default:
                                            ++data.unused;
                                            break;
                                        }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            case 's':
                                switch(iset[i++])
                                {
                                case '2':
                                    switch(iset[i++])
                                    {
                                    case 's':
                                        switch(iset[i++])
                                        {
                                        case 'i': /* cvttss2si */
                                            if (iset[i] == '\0') { ++data.sse; }
                                            break;
                                        default:
                                            ++data.unused;
                                            break;
                                        }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            default:
                ++data.unused;
                break;
            }
            break;
        case 'd':
            switch(iset[i++])
            {
            case 'i':
                switch(iset[i++])
                {
                case 'v':
                    switch(iset[i++])
                    {
                    case 'p':
                        switch(iset[i++])
                        {
                        case 'd': /* divpd */
                            if (iset[i] == '\0') { ++data.sse2; }
                            break;
                        case 's': /* divps */
                            if (iset[i] == '\0') { ++data.sse; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 's':
                        switch(iset[i++])
                        {
                        case 'd': /* divsd */
                            if (iset[i] == '\0') { ++data.sse2; }
                            break;
                        case 's': /* divss */
                            if (iset[i] == '\0') { ++data.sse; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'p':
                switch(iset[i++])
                {
                case 'p':
                    switch(iset[i++])
                    {
                    case 'd': /* dppd */
                        if (iset[i] == '\0') { ++data.sse4_1; }
                        break;
                    case 's': /* dpps */
                        if (iset[i] == '\0') { ++data.sse4_1; }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            default:
                ++data.unused;
                break;
            }
            break;
        case 'e':
            switch(iset[i++])
            {
            case 'm':
                switch(iset[i++])
                {
                case 'm':
                    switch(iset[i++])
                    {
                    case 's': /* emms */
                        if (iset[i] == '\0') { ++data.mmx; }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'x':
                switch(iset[i++])
                {
                case 't':
                    switch(iset[i++])
                    {
                    case 'r':
                        switch(iset[i++])
                        {
                        case 'a':
                            switch(iset[i++])
                            {
                            case 'c':
                                switch(iset[i++])
                                {
                                case 't':
                                    switch(iset[i++])
                                    {
                                    case 'p':
                                        switch(iset[i++])
                                        {
                                        case 's': /* extractps */
                                            if (iset[i] == '\0') { ++data.sse4_1; }
                                            break;
                                        default:
                                            ++data.unused;
                                            break;
                                        }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 'q': /* extrq */
                            if (iset[i] == '\0') { ++data.sse4a; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            default:
                ++data.unused;
                break;
            }
            break;
        case 'f':
            switch(iset[i++])
            {
            case 'c':
                switch(iset[i++])
                {
                case 'm':
                    switch(iset[i++])
                    {
                    case 'o':
                        switch(iset[i++])
                        {
                        case 'v':
                            switch(iset[i++])
                            {
                            case 'c':
                                switch(iset[i++])
                                {
                                case 'c': /* fcmovcc */
                                    if (iset[i] == '\0') { ++data.ppro; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'o':
                    switch(iset[i++])
                    {
                    case 'm':
                        switch(iset[i++])
                        {
                        case 'i': /* fcomi */
                            if (iset[i] == '\0') { ++data.ppro; }
                            else
                            switch(iset[i++])
                            {
                            case 'p': /* fcomip */
                                if (iset[i] == '\0') { ++data.ppro; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'e':
                switch(iset[i++])
                {
                case 'm':
                    switch(iset[i++])
                    {
                    case 'm':
                        switch(iset[i++])
                        {
                        case 's': /* femms */
                            if (iset[i] == '\0') { ++data.tdnow1; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'i':
                switch(iset[i++])
                {
                case 's':
                    switch(iset[i++])
                    {
                    case 't':
                        switch(iset[i++])
                        {
                        case 't':
                            switch(iset[i++])
                            {
                            case 'p': /* fisttp */
                                if (iset[i] == '\0') { ++data.sse3; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'u':
                switch(iset[i++])
                {
                case 'c':
                    switch(iset[i++])
                    {
                    case 'o':
                        switch(iset[i++])
                        {
                        case 'm':
                            switch(iset[i++])
                            {
                            case 'i': /* fucomi */
                                if (iset[i] == '\0') { ++data.ppro; }
                                else
                                switch(iset[i++])
                                {
                                case 'p': /* fucomip */
                                    if (iset[i] == '\0') { ++data.ppro; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'x':
                switch(iset[i++])
                {
                case 'r':
                    switch(iset[i++])
                    {
                    case 's':
                        switch(iset[i++])
                        {
                        case 't':
                            switch(iset[i++])
                            {
                            case 'o':
                                switch(iset[i++])
                                {
                                case 'r': /* fxrstor */
                                    if (iset[i] == '\0') { ++data.sse; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 's':
                    switch(iset[i++])
                    {
                    case 'a':
                        switch(iset[i++])
                        {
                        case 'v':
                            switch(iset[i++])
                            {
                            case 'e': /* fxsave */
                                if (iset[i] == '\0') { ++data.sse; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            default:
                ++data.unused;
                break;
            }
            break;
        case 'h':
            switch(iset[i++])
            {
            case 'a':
                switch(iset[i++])
                {
                case 'd':
                    switch(iset[i++])
                    {
                    case 'd':
                        switch(iset[i++])
                        {
                        case 'p':
                            switch(iset[i++])
                            {
                            case 'd': /* haddpd */
                                if (iset[i] == '\0') { ++data.sse3; }
                                break;
                            case 's': /* haddps */
                                if (iset[i] == '\0') { ++data.sse3; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 's':
                switch(iset[i++])
                {
                case 'u':
                    switch(iset[i++])
                    {
                    case 'b':
                        switch(iset[i++])
                        {
                        case 'p':
                            switch(iset[i++])
                            {
                            case 'd': /* hsubpd */
                                if (iset[i] == '\0') { ++data.sse3; }
                                break;
                            case 's': /* hsubps */
                                if (iset[i] == '\0') { ++data.sse3; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            default:
                ++data.unused;
                break;
            }
            break;
        case 'i':
            switch(iset[i++])
            {
            case 'n':
                switch(iset[i++])
                {
                case 's':
                    switch(iset[i++])
                    {
                    case 'e':
                        switch(iset[i++])
                        {
                        case 'r':
                            switch(iset[i++])
                            {
                            case 't':
                                switch(iset[i++])
                                {
                                case 'p':
                                    switch(iset[i++])
                                    {
                                    case 's': /* insertps */
                                        if (iset[i] == '\0') { ++data.sse4_1; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                case 'q': /* insertq */
                                    if (iset[i] == '\0') { ++data.sse4a; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'v':
                    switch(iset[i++])
                    {
                    case 'd': /* invd */
                        if (iset[i] == '\0') { ++data.i486; }
                        break;
                    case 'l':
                        switch(iset[i++])
                        {
                        case 'p':
                            switch(iset[i++])
                            {
                            case 'g': /* invlpg */
                                if (iset[i] == '\0') { ++data.i486; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            default:
                ++data.unused;
                break;
            }
            break;
        case 'l':
            switch(iset[i++])
            {
            case 'd':
                switch(iset[i++])
                {
                case 'd':
                    switch(iset[i++])
                    {
                    case 'q':
                        switch(iset[i++])
                        {
                        case 'u': /* lddqu */
                            if (iset[i] == '\0') { ++data.sse3; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'm':
                    switch(iset[i++])
                    {
                    case 'x':
                        switch(iset[i++])
                        {
                        case 'c':
                            switch(iset[i++])
                            {
                            case 's':
                                switch(iset[i++])
                                {
                                case 'r': /* ldmxcsr */
                                    if (iset[i] == '\0') { ++data.sse; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'f':
                switch(iset[i++])
                {
                case 'e':
                    switch(iset[i++])
                    {
                    case 'n':
                        switch(iset[i++])
                        {
                        case 'c':
                            switch(iset[i++])
                            {
                            case 'e': /* lfence */
                                if (iset[i] == '\0') { ++data.sse2; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'z':
                switch(iset[i++])
                {
                case 'c':
                    switch(iset[i++])
                    {
                    case 'n':
                        switch(iset[i++])
                        {
                        case 't': /* lzcnt */
                            if (iset[i] == '\0') { ++data.sse4a; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            default:
                ++data.unused;
                break;
            }
            break;
        case 'm':
            switch(iset[i++])
            {
            case 'a':
                switch(iset[i++])
                {
                case 's':
                    switch(iset[i++])
                    {
                    case 'k':
                        switch(iset[i++])
                        {
                        case 'm':
                            switch(iset[i++])
                            {
                            case 'o':
                                switch(iset[i++])
                                {
                                case 'v':
                                    switch(iset[i++])
                                    {
                                    case 'd':
                                        switch(iset[i++])
                                        {
                                        case 'q':
                                            switch(iset[i++])
                                            {
                                            case 'u': /* maskmovdqu */
                                                if (iset[i] == '\0') { ++data.sse2; }
                                                break;
                                            default:
                                                ++data.unused;
                                                break;
                                            }
                                            break;
                                        default:
                                            ++data.unused;
                                            break;
                                        }
                                        break;
                                    case 'q': /* maskmovq */
                                        if (iset[i] == '\0') { ++data.sse;++data.tdnow2; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'x':
                    switch(iset[i++])
                    {
                    case 'p':
                        switch(iset[i++])
                        {
                        case 'd': /* maxpd */
                            if (iset[i] == '\0') { ++data.sse2; }
                            break;
                        case 's': /* maxps */
                            if (iset[i] == '\0') { ++data.sse; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 's':
                        switch(iset[i++])
                        {
                        case 'd': /* maxsd */
                            if (iset[i] == '\0') { ++data.sse2; }
                            break;
                        case 's': /* maxss */
                            if (iset[i] == '\0') { ++data.sse; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'f':
                switch(iset[i++])
                {
                case 'e':
                    switch(iset[i++])
                    {
                    case 'n':
                        switch(iset[i++])
                        {
                        case 'c':
                            switch(iset[i++])
                            {
                            case 'e': /* mfence */
                                if (iset[i] == '\0') { ++data.sse2; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'i':
                switch(iset[i++])
                {
                case 'n':
                    switch(iset[i++])
                    {
                    case 'p':
                        switch(iset[i++])
                        {
                        case 'd': /* minpd */
                            if (iset[i] == '\0') { ++data.sse2; }
                            break;
                        case 's': /* minps */
                            if (iset[i] == '\0') { ++data.sse; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 's':
                        switch(iset[i++])
                        {
                        case 'd': /* minsd */
                            if (iset[i] == '\0') { ++data.sse2; }
                            break;
                        case 's': /* minss */
                            if (iset[i] == '\0') { ++data.sse; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'o':
                switch(iset[i++])
                {
                case 'n':
                    switch(iset[i++])
                    {
                    case 'i':
                        switch(iset[i++])
                        {
                        case 't':
                            switch(iset[i++])
                            {
                            case 'o':
                                switch(iset[i++])
                                {
                                case 'r': /* monitor */
                                    if (iset[i] == '\0') { ++data.sse3; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'v':
                    switch(iset[i++])
                    {
                    case 'a':
                        switch(iset[i++])
                        {
                        case 'p':
                            switch(iset[i++])
                            {
                            case 'd': /* movapd */
                                if (iset[i] == '\0') { ++data.sse2; }
                                break;
                            case 's': /* movaps */
                                if (iset[i] == '\0') { ++data.sse; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 'd': /* movd */
                        if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                        else
                        switch(iset[i++])
                        {
                        case 'd':
                            switch(iset[i++])
                            {
                            case 'u':
                                switch(iset[i++])
                                {
                                case 'p': /* movddup */
                                    if (iset[i] == '\0') { ++data.sse3; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 'q':
                            switch(iset[i++])
                            {
                            case '2':
                                switch(iset[i++])
                                {
                                case 'q': /* movdq2q */
                                    if (iset[i] == '\0') { ++data.sse2; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            case 'a': /* movdqa */
                                if (iset[i] == '\0') { ++data.sse2; }
                                break;
                            case 'u': /* movdqu */
                                if (iset[i] == '\0') { ++data.sse2; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 'h':
                        switch(iset[i++])
                        {
                        case 'l':
                            switch(iset[i++])
                            {
                            case 'p':
                                switch(iset[i++])
                                {
                                case 's': /* movhlps */
                                    if (iset[i] == '\0') { ++data.sse; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 'p':
                            switch(iset[i++])
                            {
                            case 'd': /* movhpd */
                                if (iset[i] == '\0') { ++data.sse2; }
                                break;
                            case 's': /* movhps */
                                if (iset[i] == '\0') { ++data.sse; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 'l':
                        switch(iset[i++])
                        {
                        case 'h':
                            switch(iset[i++])
                            {
                            case 'p':
                                switch(iset[i++])
                                {
                                case 's': /* movlhps */
                                    if (iset[i] == '\0') { ++data.sse; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 'p':
                            switch(iset[i++])
                            {
                            case 'd': /* movlpd */
                                if (iset[i] == '\0') { ++data.sse2; }
                                break;
                            case 's': /* movlps */
                                if (iset[i] == '\0') { ++data.sse; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 'm':
                        switch(iset[i++])
                        {
                        case 's':
                            switch(iset[i++])
                            {
                            case 'k':
                                switch(iset[i++])
                                {
                                case 'p':
                                    switch(iset[i++])
                                    {
                                    case 'd': /* movmskpd */
                                        if (iset[i] == '\0') { ++data.sse2; }
                                        break;
                                    case 's': /* movmskps */
                                        if (iset[i] == '\0') { ++data.sse; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 'n':
                        switch(iset[i++])
                        {
                        case 't':
                            switch(iset[i++])
                            {
                            case 'd':
                                switch(iset[i++])
                                {
                                case 'q': /* movntdq */
                                    if (iset[i] == '\0') { ++data.sse2; }
                                    else
                                    switch(iset[i++])
                                    {
                                    case 'a': /* movntdqa */
                                        if (iset[i] == '\0') { ++data.sse4_1; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            case 'i': /* movnti */
                                if (iset[i] == '\0') { ++data.sse2; }
                                break;
                            case 'p':
                                switch(iset[i++])
                                {
                                case 'd': /* movntpd */
                                    if (iset[i] == '\0') { ++data.sse2; }
                                    break;
                                case 's': /* movntps */
                                    if (iset[i] == '\0') { ++data.sse; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            case 'q': /* movntq */
                                if (iset[i] == '\0') { ++data.sse;++data.tdnow2; }
                                break;
                            case 's':
                                switch(iset[i++])
                                {
                                case 'd': /* movntsd */
                                    if (iset[i] == '\0') { ++data.sse4a; }
                                    break;
                                case 's': /* movntss */
                                    if (iset[i] == '\0') { ++data.sse4a; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 'q': /* movq */
                        if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                        else
                        switch(iset[i++])
                        {
                        case '2':
                            switch(iset[i++])
                            {
                            case 'd':
                                switch(iset[i++])
                                {
                                case 'q': /* movq2dq */
                                    if (iset[i] == '\0') { ++data.sse2; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 's':
                        switch(iset[i++])
                        {
                        case 'd': /* movsd */
                            if (iset[i] == '\0') { ++data.sse2; }
                            break;
                        case 'h':
                            switch(iset[i++])
                            {
                            case 'd':
                                switch(iset[i++])
                                {
                                case 'u':
                                    switch(iset[i++])
                                    {
                                    case 'p': /* movshdup */
                                        if (iset[i] == '\0') { ++data.sse3; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 'l':
                            switch(iset[i++])
                            {
                            case 'd':
                                switch(iset[i++])
                                {
                                case 'u':
                                    switch(iset[i++])
                                    {
                                    case 'p': /* movsldup */
                                        if (iset[i] == '\0') { ++data.sse3; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 's': /* movss */
                            if (iset[i] == '\0') { ++data.sse; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 'u':
                        switch(iset[i++])
                        {
                        case 'p':
                            switch(iset[i++])
                            {
                            case 'd': /* movupd */
                                if (iset[i] == '\0') { ++data.sse2; }
                                break;
                            case 's': /* movups */
                                if (iset[i] == '\0') { ++data.sse; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'p':
                switch(iset[i++])
                {
                case 's':
                    switch(iset[i++])
                    {
                    case 'a':
                        switch(iset[i++])
                        {
                        case 'd':
                            switch(iset[i++])
                            {
                            case 'b':
                                switch(iset[i++])
                                {
                                case 'w': /* mpsadbw */
                                    if (iset[i] == '\0') { ++data.sse4_1; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'u':
                switch(iset[i++])
                {
                case 'l':
                    switch(iset[i++])
                    {
                    case 'p':
                        switch(iset[i++])
                        {
                        case 'd': /* mulpd */
                            if (iset[i] == '\0') { ++data.sse2; }
                            break;
                        case 's': /* mulps */
                            if (iset[i] == '\0') { ++data.sse; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 's':
                        switch(iset[i++])
                        {
                        case 'd': /* mulsd */
                            if (iset[i] == '\0') { ++data.sse2; }
                            break;
                        case 's': /* mulss */
                            if (iset[i] == '\0') { ++data.sse; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'w':
                switch(iset[i++])
                {
                case 'a':
                    switch(iset[i++])
                    {
                    case 'i':
                        switch(iset[i++])
                        {
                        case 't': /* mwait */
                            if (iset[i] == '\0') { ++data.sse3; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            default:
                ++data.unused;
                break;
            }
            break;
        case 'o':
            switch(iset[i++])
            {
            case 'r':
                switch(iset[i++])
                {
                case 'p':
                    switch(iset[i++])
                    {
                    case 'd': /* orpd */
                        if (iset[i] == '\0') { ++data.sse2; }
                        break;
                    case 's': /* orps */
                        if (iset[i] == '\0') { ++data.sse; }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            default:
                ++data.unused;
                break;
            }
            break;
        case 'p':
            switch(iset[i++])
            {
            case 'a':
                switch(iset[i++])
                {
                case 'b':
                    switch(iset[i++])
                    {
                    case 's':
                        switch(iset[i++])
                        {
                        case 'b': /* pabsb */
                            if (iset[i] == '\0') { ++data.ssse3; }
                            break;
                        case 'd': /* pabsd */
                            if (iset[i] == '\0') { ++data.ssse3; }
                            break;
                        case 'w': /* pabsw */
                            if (iset[i] == '\0') { ++data.ssse3; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'c':
                    switch(iset[i++])
                    {
                    case 'k':
                        switch(iset[i++])
                        {
                        case 's':
                            switch(iset[i++])
                            {
                            case 's':
                                switch(iset[i++])
                                {
                                case 'd':
                                    switch(iset[i++])
                                    {
                                    case 'w': /* packssdw */
                                        if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                case 'w':
                                    switch(iset[i++])
                                    {
                                    case 'b': /* packsswb */
                                        if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 'u':
                            switch(iset[i++])
                            {
                            case 's':
                                switch(iset[i++])
                                {
                                case 'd':
                                    switch(iset[i++])
                                    {
                                    case 'w': /* packusdw */
                                        if (iset[i] == '\0') { ++data.sse4_1; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                case 'w':
                                    switch(iset[i++])
                                    {
                                    case 'b': /* packuswb */
                                        if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'd':
                    switch(iset[i++])
                    {
                    case 'd':
                        switch(iset[i++])
                        {
                        case 'b': /* paddb */
                            if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                            break;
                        case 'd': /* paddd */
                            if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                            break;
                        case 'q': /* paddq */
                            if (iset[i] == '\0') { ++data.sse2; }
                            break;
                        case 's':
                            switch(iset[i++])
                            {
                            case 'b': /* paddsb */
                                if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                break;
                            case 'w': /* paddsw */
                                if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 'u':
                            switch(iset[i++])
                            {
                            case 's':
                                switch(iset[i++])
                                {
                                case 'b': /* paddusb */
                                    if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                    break;
                                case 'w': /* paddusw */
                                    if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 'w': /* paddw */
                            if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'l':
                    switch(iset[i++])
                    {
                    case 'i':
                        switch(iset[i++])
                        {
                        case 'g':
                            switch(iset[i++])
                            {
                            case 'n':
                                switch(iset[i++])
                                {
                                case 'r': /* palignr */
                                    if (iset[i] == '\0') { ++data.ssse3; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'n':
                    switch(iset[i++])
                    {
                    case 'd': /* pand */
                        if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                        else
                        switch(iset[i++])
                        {
                        case 'n': /* pandn */
                            if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'u':
                    switch(iset[i++])
                    {
                    case 's':
                        switch(iset[i++])
                        {
                        case 'e': /* pause */
                            if (iset[i] == '\0') { ++data.sse2; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'v':
                    switch(iset[i++])
                    {
                    case 'g':
                        switch(iset[i++])
                        {
                        case 'b': /* pavgb */
                            if (iset[i] == '\0') { ++data.sse;++data.sse2;++data.tdnow2; }
                            break;
                        case 'u':
                            switch(iset[i++])
                            {
                            case 's':
                                switch(iset[i++])
                                {
                                case 'b': /* pavgusb */
                                    if (iset[i] == '\0') { ++data.tdnow1; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 'w': /* pavgw */
                            if (iset[i] == '\0') { ++data.sse;++data.sse2;++data.tdnow2; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'b':
                switch(iset[i++])
                {
                case 'l':
                    switch(iset[i++])
                    {
                    case 'e':
                        switch(iset[i++])
                        {
                        case 'n':
                            switch(iset[i++])
                            {
                            case 'd':
                                switch(iset[i++])
                                {
                                case 'v':
                                    switch(iset[i++])
                                    {
                                    case 'b': /* pblendvb */
                                        if (iset[i] == '\0') { ++data.sse4_1; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                case 'w': /* pblendw */
                                    if (iset[i] == '\0') { ++data.sse4_1; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'c':
                switch(iset[i++])
                {
                case 'm':
                    switch(iset[i++])
                    {
                    case 'p':
                        switch(iset[i++])
                        {
                        case 'e':
                            switch(iset[i++])
                            {
                            case 'q':
                                switch(iset[i++])
                                {
                                case 'b': /* pcmpeqb */
                                    if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                    break;
                                case 'd': /* pcmpeqd */
                                    if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                    break;
                                case 'q': /* pcmpeqq */
                                    if (iset[i] == '\0') { ++data.sse4_1; }
                                    break;
                                case 'w': /* pcmpeqw */
                                    if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            case 's':
                                switch(iset[i++])
                                {
                                case 't':
                                    switch(iset[i++])
                                    {
                                    case 'r':
                                        switch(iset[i++])
                                        {
                                        case 'i': /* pcmpestri */
                                            if (iset[i] == '\0') { ++data.sse4_2; }
                                            break;
                                        case 'm': /* pcmpestrm */
                                            if (iset[i] == '\0') { ++data.sse4_2; }
                                            break;
                                        default:
                                            ++data.unused;
                                            break;
                                        }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 'g':
                            switch(iset[i++])
                            {
                            case 't':
                                switch(iset[i++])
                                {
                                case 'b': /* pcmpgtb */
                                    if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                    break;
                                case 'd': /* pcmpgtd */
                                    if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                    break;
                                case 'q': /* pcmpgtq */
                                    if (iset[i] == '\0') { ++data.sse4_2; }
                                    break;
                                case 'w': /* pcmpgtw */
                                    if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 'i':
                            switch(iset[i++])
                            {
                            case 's':
                                switch(iset[i++])
                                {
                                case 't':
                                    switch(iset[i++])
                                    {
                                    case 'r':
                                        switch(iset[i++])
                                        {
                                        case 'i': /* pcmpistri */
                                            if (iset[i] == '\0') { ++data.sse4_2; }
                                            break;
                                        case 'm': /* pcmpistrm */
                                            if (iset[i] == '\0') { ++data.sse4_2; }
                                            break;
                                        default:
                                            ++data.unused;
                                            break;
                                        }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'e':
                switch(iset[i++])
                {
                case 'x':
                    switch(iset[i++])
                    {
                    case 't':
                        switch(iset[i++])
                        {
                        case 'r':
                            switch(iset[i++])
                            {
                            case 'b': /* pextrb */
                                if (iset[i] == '\0') { ++data.sse4_1; }
                                break;
                            case 'd': /* pextrd */
                                if (iset[i] == '\0') { ++data.sse4_1; }
                                break;
                            case 'q': /* pextrq */
                                if (iset[i] == '\0') { ++data.sse4_1; }
                                break;
                            case 'w': /* pextrw */
                                if (iset[i] == '\0') { ++data.sse;++data.sse2;++data.sse4_1;++data.tdnow2; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'f':
                switch(iset[i++])
                {
                case '2':
                    switch(iset[i++])
                    {
                    case 'i':
                        switch(iset[i++])
                        {
                        case 'd': /* pf2id */
                            if (iset[i] == '\0') { ++data.tdnow1; }
                            break;
                        case 'w': /* pf2iw */
                            if (iset[i] == '\0') { ++data.tdnow1;++data.tdnow2; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'a':
                    switch(iset[i++])
                    {
                    case 'c':
                        switch(iset[i++])
                        {
                        case 'c': /* pfacc */
                            if (iset[i] == '\0') { ++data.tdnow1; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 'd':
                        switch(iset[i++])
                        {
                        case 'd': /* pfadd */
                            if (iset[i] == '\0') { ++data.tdnow1; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'c':
                    switch(iset[i++])
                    {
                    case 'm':
                        switch(iset[i++])
                        {
                        case 'p':
                            switch(iset[i++])
                            {
                            case 'e':
                                switch(iset[i++])
                                {
                                case 'q': /* pfcmpeq */
                                    if (iset[i] == '\0') { ++data.tdnow1; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            case 'g':
                                switch(iset[i++])
                                {
                                case 'e': /* pfcmpge */
                                    if (iset[i] == '\0') { ++data.tdnow1; }
                                    break;
                                case 't': /* pfcmpgt */
                                    if (iset[i] == '\0') { ++data.tdnow1; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'm':
                    switch(iset[i++])
                    {
                    case 'a':
                        switch(iset[i++])
                        {
                        case 'x': /* pfmax */
                            if (iset[i] == '\0') { ++data.tdnow1; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 'i':
                        switch(iset[i++])
                        {
                        case 'n': /* pfmin */
                            if (iset[i] == '\0') { ++data.tdnow1; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 'u':
                        switch(iset[i++])
                        {
                        case 'l': /* pfmul */
                            if (iset[i] == '\0') { ++data.tdnow1; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'n':
                    switch(iset[i++])
                    {
                    case 'a':
                        switch(iset[i++])
                        {
                        case 'c':
                            switch(iset[i++])
                            {
                            case 'c': /* pfnacc */
                                if (iset[i] == '\0') { ++data.tdnow2; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'p':
                    switch(iset[i++])
                    {
                    case 'n':
                        switch(iset[i++])
                        {
                        case 'a':
                            switch(iset[i++])
                            {
                            case 'c':
                                switch(iset[i++])
                                {
                                case 'c': /* pfpnacc */
                                    if (iset[i] == '\0') { ++data.tdnow2; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'r':
                    switch(iset[i++])
                    {
                    case 'c':
                        switch(iset[i++])
                        {
                        case 'p': /* pfrcp */
                            if (iset[i] == '\0') { ++data.tdnow1; }
                            else
                            switch(iset[i++])
                            {
                            case 'i':
                                switch(iset[i++])
                                {
                                case 't':
                                    switch(iset[i++])
                                    {
                                    case '1': /* pfrcpit1 */
                                        if (iset[i] == '\0') { ++data.tdnow1; }
                                        break;
                                    case '2': /* pfrcpit2 */
                                        if (iset[i] == '\0') { ++data.tdnow1; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 's':
                        switch(iset[i++])
                        {
                        case 'q':
                            switch(iset[i++])
                            {
                            case 'i':
                                switch(iset[i++])
                                {
                                case 't':
                                    switch(iset[i++])
                                    {
                                    case '1': /* pfrsqit1 */
                                        if (iset[i] == '\0') { ++data.tdnow1; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            case 'r':
                                switch(iset[i++])
                                {
                                case 't': /* pfrsqrt */
                                    if (iset[i] == '\0') { ++data.tdnow1; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 's':
                    switch(iset[i++])
                    {
                    case 'u':
                        switch(iset[i++])
                        {
                        case 'b': /* pfsub */
                            if (iset[i] == '\0') { ++data.tdnow1; }
                            else
                            switch(iset[i++])
                            {
                            case 'r': /* pfsubr */
                                if (iset[i] == '\0') { ++data.tdnow1; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'h':
                switch(iset[i++])
                {
                case 'a':
                    switch(iset[i++])
                    {
                    case 'd':
                        switch(iset[i++])
                        {
                        case 'd':
                            switch(iset[i++])
                            {
                            case 'd': /* phaddd */
                                if (iset[i] == '\0') { ++data.ssse3; }
                                break;
                            case 's':
                                switch(iset[i++])
                                {
                                case 'w': /* phaddsw */
                                    if (iset[i] == '\0') { ++data.ssse3; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            case 'w': /* phaddw */
                                if (iset[i] == '\0') { ++data.ssse3; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'm':
                    switch(iset[i++])
                    {
                    case 'i':
                        switch(iset[i++])
                        {
                        case 'n':
                            switch(iset[i++])
                            {
                            case 'p':
                                switch(iset[i++])
                                {
                                case 'o':
                                    switch(iset[i++])
                                    {
                                    case 's':
                                        switch(iset[i++])
                                        {
                                        case 'u':
                                            switch(iset[i++])
                                            {
                                            case 'w': /* phminposuw */
                                                if (iset[i] == '\0') { ++data.sse4_1; }
                                                break;
                                            default:
                                                ++data.unused;
                                                break;
                                            }
                                            break;
                                        default:
                                            ++data.unused;
                                            break;
                                        }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 's':
                    switch(iset[i++])
                    {
                    case 'u':
                        switch(iset[i++])
                        {
                        case 'b':
                            switch(iset[i++])
                            {
                            case 'd': /* phsubd */
                                if (iset[i] == '\0') { ++data.ssse3; }
                                break;
                            case 's':
                                switch(iset[i++])
                                {
                                case 'w': /* phsubsw */
                                    if (iset[i] == '\0') { ++data.ssse3; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            case 'w': /* phsubw */
                                if (iset[i] == '\0') { ++data.ssse3; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'i':
                switch(iset[i++])
                {
                case '2':
                    switch(iset[i++])
                    {
                    case 'f':
                        switch(iset[i++])
                        {
                        case 'd': /* pi2fd */
                            if (iset[i] == '\0') { ++data.tdnow1; }
                            break;
                        case 'w': /* pi2fw */
                            if (iset[i] == '\0') { ++data.tdnow1;++data.tdnow2; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'n':
                    switch(iset[i++])
                    {
                    case 's':
                        switch(iset[i++])
                        {
                        case 'r':
                            switch(iset[i++])
                            {
                            case 'b': /* pinsrb */
                                if (iset[i] == '\0') { ++data.sse4_1; }
                                break;
                            case 'd': /* pinsrd */
                                if (iset[i] == '\0') { ++data.sse4_1; }
                                break;
                            case 'q': /* pinsrq */
                                if (iset[i] == '\0') { ++data.sse4_1; }
                                break;
                            case 'w': /* pinsrw */
                                if (iset[i] == '\0') { ++data.sse;++data.sse2;++data.tdnow2; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'm':
                switch(iset[i++])
                {
                case 'a':
                    switch(iset[i++])
                    {
                    case 'd':
                        switch(iset[i++])
                        {
                        case 'd':
                            switch(iset[i++])
                            {
                            case 'u':
                                switch(iset[i++])
                                {
                                case 'b':
                                    switch(iset[i++])
                                    {
                                    case 's':
                                        switch(iset[i++])
                                        {
                                        case 'w': /* pmaddubsw */
                                            if (iset[i] == '\0') { ++data.ssse3; }
                                            break;
                                        default:
                                            ++data.unused;
                                            break;
                                        }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            case 'w':
                                switch(iset[i++])
                                {
                                case 'd': /* pmaddwd */
                                    if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 'x':
                        switch(iset[i++])
                        {
                        case 's':
                            switch(iset[i++])
                            {
                            case 'b': /* pmaxsb */
                                if (iset[i] == '\0') { ++data.sse4_1; }
                                break;
                            case 'd': /* pmaxsd */
                                if (iset[i] == '\0') { ++data.sse4_1; }
                                break;
                            case 'w': /* pmaxsw */
                                if (iset[i] == '\0') { ++data.sse;++data.sse2;++data.tdnow2; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 'u':
                            switch(iset[i++])
                            {
                            case 'b': /* pmaxub */
                                if (iset[i] == '\0') { ++data.sse;++data.sse2;++data.tdnow2; }
                                break;
                            case 'd': /* pmaxud */
                                if (iset[i] == '\0') { ++data.sse4_1; }
                                break;
                            case 'w': /* pmaxuw */
                                if (iset[i] == '\0') { ++data.sse4_1; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'i':
                    switch(iset[i++])
                    {
                    case 'n':
                        switch(iset[i++])
                        {
                        case 's':
                            switch(iset[i++])
                            {
                            case 'b': /* pminsb */
                                if (iset[i] == '\0') { ++data.sse4_1; }
                                break;
                            case 'd': /* pminsd */
                                if (iset[i] == '\0') { ++data.sse4_1; }
                                break;
                            case 'w': /* pminsw */
                                if (iset[i] == '\0') { ++data.sse;++data.sse2;++data.tdnow2; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 'u':
                            switch(iset[i++])
                            {
                            case 'b': /* pminub */
                                if (iset[i] == '\0') { ++data.sse;++data.sse2;++data.tdnow2; }
                                break;
                            case 'd': /* pminud */
                                if (iset[i] == '\0') { ++data.sse4_1; }
                                break;
                            case 'w': /* pminuw */
                                if (iset[i] == '\0') { ++data.sse4_1; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'o':
                    switch(iset[i++])
                    {
                    case 'v':
                        switch(iset[i++])
                        {
                        case 'm':
                            switch(iset[i++])
                            {
                            case 's':
                                switch(iset[i++])
                                {
                                case 'k':
                                    switch(iset[i++])
                                    {
                                    case 'b': /* pmovmskb */
                                        if (iset[i] == '\0') { ++data.sse;++data.sse2;++data.tdnow2; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 's':
                            switch(iset[i++])
                            {
                            case 'x':
                                switch(iset[i++])
                                {
                                case 'b':
                                    switch(iset[i++])
                                    {
                                    case 'd': /* pmovsxbd */
                                        if (iset[i] == '\0') { ++data.sse4_1; }
                                        break;
                                    case 'q': /* pmovsxbq */
                                        if (iset[i] == '\0') { ++data.sse4_1; }
                                        break;
                                    case 'w': /* pmovsxbw */
                                        if (iset[i] == '\0') { ++data.sse4_1; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                case 'd':
                                    switch(iset[i++])
                                    {
                                    case 'q': /* pmovsxdq */
                                        if (iset[i] == '\0') { ++data.sse4_1; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                case 'w':
                                    switch(iset[i++])
                                    {
                                    case 'd': /* pmovsxwd */
                                        if (iset[i] == '\0') { ++data.sse4_1; }
                                        break;
                                    case 'q': /* pmovsxwq */
                                        if (iset[i] == '\0') { ++data.sse4_1; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 'z':
                            switch(iset[i++])
                            {
                            case 'x':
                                switch(iset[i++])
                                {
                                case 'b':
                                    switch(iset[i++])
                                    {
                                    case 'd': /* pmovzxbd */
                                        if (iset[i] == '\0') { ++data.sse4_1; }
                                        break;
                                    case 'q': /* pmovzxbq */
                                        if (iset[i] == '\0') { ++data.sse4_1; }
                                        break;
                                    case 'w': /* pmovzxbw */
                                        if (iset[i] == '\0') { ++data.sse4_1; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                case 'd':
                                    switch(iset[i++])
                                    {
                                    case 'q': /* pmovzxdq */
                                        if (iset[i] == '\0') { ++data.sse4_1; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                case 'w':
                                    switch(iset[i++])
                                    {
                                    case 'd': /* pmovzxwd */
                                        if (iset[i] == '\0') { ++data.sse4_1; }
                                        break;
                                    case 'q': /* pmovzxwq */
                                        if (iset[i] == '\0') { ++data.sse4_1; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'u':
                    switch(iset[i++])
                    {
                    case 'l':
                        switch(iset[i++])
                        {
                        case 'd':
                            switch(iset[i++])
                            {
                            case 'q': /* pmuldq */
                                if (iset[i] == '\0') { ++data.sse4_1; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 'h':
                            switch(iset[i++])
                            {
                            case 'r':
                                switch(iset[i++])
                                {
                                case 's':
                                    switch(iset[i++])
                                    {
                                    case 'w': /* pmulhrsw */
                                        if (iset[i] == '\0') { ++data.ssse3; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                case 'w': /* pmulhrw */
                                    if (iset[i] == '\0') { ++data.tdnow1; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            case 'u':
                                switch(iset[i++])
                                {
                                case 'w': /* pmulhuw */
                                    if (iset[i] == '\0') { ++data.sse;++data.sse2;++data.tdnow2; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            case 'w': /* pmulhw */
                                if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 'l':
                            switch(iset[i++])
                            {
                            case 'd': /* pmulld */
                                if (iset[i] == '\0') { ++data.sse4_1; }
                                break;
                            case 'w': /* pmullw */
                                if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 'u':
                            switch(iset[i++])
                            {
                            case 'd':
                                switch(iset[i++])
                                {
                                case 'q': /* pmuludq */
                                    if (iset[i] == '\0') { ++data.sse2; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'o':
                switch(iset[i++])
                {
                case 'p':
                    switch(iset[i++])
                    {
                    case 'c':
                        switch(iset[i++])
                        {
                        case 'n':
                            switch(iset[i++])
                            {
                            case 't': /* popcnt */
                                if (iset[i] == '\0') { ++data.sse4_2;++data.sse4a; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'r': /* por */
                    if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'r':
                switch(iset[i++])
                {
                case 'e':
                    switch(iset[i++])
                    {
                    case 'f':
                        switch(iset[i++])
                        {
                        case 'e':
                            switch(iset[i++])
                            {
                            case 't':
                                switch(iset[i++])
                                {
                                case 'c':
                                    switch(iset[i++])
                                    {
                                    case 'h': /* prefetch */
                                        if (iset[i] == '\0') { ++data.sse;++data.tdnow1; }
                                        else
                                        switch(iset[i++])
                                        {
                                        case 'n':
                                            switch(iset[i++])
                                            {
                                            case 't':
                                                switch(iset[i++])
                                                {
                                                case 'a': /* prefetchnta */
                                                    if (iset[i] == '\0') { ++data.tdnow2; }
                                                    break;
                                                default:
                                                    ++data.unused;
                                                    break;
                                                }
                                                break;
                                            default:
                                                ++data.unused;
                                                break;
                                            }
                                            break;
                                        case 't':
                                            switch(iset[i++])
                                            {
                                            case '0': /* prefetcht0 */
                                                if (iset[i] == '\0') { ++data.tdnow2; }
                                                break;
                                            case '1': /* prefetcht1 */
                                                if (iset[i] == '\0') { ++data.tdnow2; }
                                                break;
                                            case '2': /* prefetcht2 */
                                                if (iset[i] == '\0') { ++data.tdnow2; }
                                                break;
                                            default:
                                                ++data.unused;
                                                break;
                                            }
                                            break;
                                        default:
                                            ++data.unused;
                                            break;
                                        }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 's':
                switch(iset[i++])
                {
                case 'a':
                    switch(iset[i++])
                    {
                    case 'd':
                        switch(iset[i++])
                        {
                        case 'b':
                            switch(iset[i++])
                            {
                            case 'w': /* psadbw */
                                if (iset[i] == '\0') { ++data.sse;++data.sse2;++data.tdnow2; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'h':
                    switch(iset[i++])
                    {
                    case 'u':
                        switch(iset[i++])
                        {
                        case 'f':
                            switch(iset[i++])
                            {
                            case 'b': /* pshufb */
                                if (iset[i] == '\0') { ++data.ssse3; }
                                break;
                            case 'd': /* pshufd */
                                if (iset[i] == '\0') { ++data.sse2; }
                                break;
                            case 'h':
                                switch(iset[i++])
                                {
                                case 'w': /* pshufhw */
                                    if (iset[i] == '\0') { ++data.sse2; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            case 'l':
                                switch(iset[i++])
                                {
                                case 'w': /* pshuflw */
                                    if (iset[i] == '\0') { ++data.sse2; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            case 'w': /* pshufw */
                                if (iset[i] == '\0') { ++data.sse;++data.tdnow2; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'i':
                    switch(iset[i++])
                    {
                    case 'g':
                        switch(iset[i++])
                        {
                        case 'n':
                            switch(iset[i++])
                            {
                            case 'b': /* psignb */
                                if (iset[i] == '\0') { ++data.ssse3; }
                                break;
                            case 'd': /* psignd */
                                if (iset[i] == '\0') { ++data.ssse3; }
                                break;
                            case 'w': /* psignw */
                                if (iset[i] == '\0') { ++data.ssse3; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'l':
                    switch(iset[i++])
                    {
                    case 'l':
                        switch(iset[i++])
                        {
                        case 'd': /* pslld */
                            if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                            else
                            switch(iset[i++])
                            {
                            case 'q': /* pslldq */
                                if (iset[i] == '\0') { ++data.sse2; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 'q': /* psllq */
                            if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                            break;
                        case 'w': /* psllw */
                            if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'r':
                    switch(iset[i++])
                    {
                    case 'a':
                        switch(iset[i++])
                        {
                        case 'd': /* psrad */
                            if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                            break;
                        case 'w': /* psraw */
                            if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 'l':
                        switch(iset[i++])
                        {
                        case 'd': /* psrld */
                            if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                            else
                            switch(iset[i++])
                            {
                            case 'q': /* psrldq */
                                if (iset[i] == '\0') { ++data.sse2; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 'q': /* psrlq */
                            if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                            break;
                        case 'w': /* psrlw */
                            if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'u':
                    switch(iset[i++])
                    {
                    case 'b':
                        switch(iset[i++])
                        {
                        case 'b': /* psubb */
                            if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                            break;
                        case 'd': /* psubd */
                            if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                            break;
                        case 'q': /* psubq */
                            if (iset[i] == '\0') { ++data.sse2; }
                            break;
                        case 's':
                            switch(iset[i++])
                            {
                            case 'b': /* psubsb */
                                if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                break;
                            case 'w': /* psubsw */
                                if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 'u':
                            switch(iset[i++])
                            {
                            case 's':
                                switch(iset[i++])
                                {
                                case 'b': /* psubusb */
                                    if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                    break;
                                case 'w': /* psubusw */
                                    if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 'w': /* psubw */
                            if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'w':
                    switch(iset[i++])
                    {
                    case 'a':
                        switch(iset[i++])
                        {
                        case 'p':
                            switch(iset[i++])
                            {
                            case 'd': /* pswapd */
                                if (iset[i] == '\0') { ++data.tdnow2; }
                                break;
                            case 'w': /* pswapw */
                                if (iset[i] == '\0') { ++data.tdnow1; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 't':
                switch(iset[i++])
                {
                case 'e':
                    switch(iset[i++])
                    {
                    case 's':
                        switch(iset[i++])
                        {
                        case 't': /* ptest */
                            if (iset[i] == '\0') { ++data.sse4_1; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'u':
                switch(iset[i++])
                {
                case 'n':
                    switch(iset[i++])
                    {
                    case 'p':
                        switch(iset[i++])
                        {
                        case 'c':
                            switch(iset[i++])
                            {
                            case 'k':
                                switch(iset[i++])
                                {
                                case 'h':
                                    switch(iset[i++])
                                    {
                                    case 'b':
                                        switch(iset[i++])
                                        {
                                        case 'w': /* punpckhbw */
                                            if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                            break;
                                        default:
                                            ++data.unused;
                                            break;
                                        }
                                        break;
                                    case 'd':
                                        switch(iset[i++])
                                        {
                                        case 'q': /* punpckhdq */
                                            if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                            break;
                                        default:
                                            ++data.unused;
                                            break;
                                        }
                                        break;
                                    case 'q':
                                        switch(iset[i++])
                                        {
                                        case 'd':
                                            switch(iset[i++])
                                            {
                                            case 'q': /* punpckhqdq */
                                                if (iset[i] == '\0') { ++data.sse2; }
                                                break;
                                            default:
                                                ++data.unused;
                                                break;
                                            }
                                            break;
                                        default:
                                            ++data.unused;
                                            break;
                                        }
                                        break;
                                    case 'w':
                                        switch(iset[i++])
                                        {
                                        case 'd': /* punpckhwd */
                                            if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                            break;
                                        default:
                                            ++data.unused;
                                            break;
                                        }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                case 'l':
                                    switch(iset[i++])
                                    {
                                    case 'b':
                                        switch(iset[i++])
                                        {
                                        case 'w': /* punpcklbw */
                                            if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                            break;
                                        default:
                                            ++data.unused;
                                            break;
                                        }
                                        break;
                                    case 'd':
                                        switch(iset[i++])
                                        {
                                        case 'q': /* punpckldq */
                                            if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                            break;
                                        default:
                                            ++data.unused;
                                            break;
                                        }
                                        break;
                                    case 'q':
                                        switch(iset[i++])
                                        {
                                        case 'd':
                                            switch(iset[i++])
                                            {
                                            case 'q': /* punpcklqdq */
                                                if (iset[i] == '\0') { ++data.sse2; }
                                                break;
                                            default:
                                                ++data.unused;
                                                break;
                                            }
                                            break;
                                        default:
                                            ++data.unused;
                                            break;
                                        }
                                        break;
                                    case 'w':
                                        switch(iset[i++])
                                        {
                                        case 'd': /* punpcklwd */
                                            if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                                            break;
                                        default:
                                            ++data.unused;
                                            break;
                                        }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'x':
                switch(iset[i++])
                {
                case 'o':
                    switch(iset[i++])
                    {
                    case 'r': /* pxor */
                        if (iset[i] == '\0') { ++data.mmx;++data.sse2; }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            default:
                ++data.unused;
                break;
            }
            break;
        case 'r':
            switch(iset[i++])
            {
            case 'c':
                switch(iset[i++])
                {
                case 'p':
                    switch(iset[i++])
                    {
                    case 'p':
                        switch(iset[i++])
                        {
                        case 's': /* rcpps */
                            if (iset[i] == '\0') { ++data.sse; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 's':
                        switch(iset[i++])
                        {
                        case 's': /* rcpss */
                            if (iset[i] == '\0') { ++data.sse; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'd':
                switch(iset[i++])
                {
                case 'm':
                    switch(iset[i++])
                    {
                    case 's':
                        switch(iset[i++])
                        {
                        case 'r': /* rdmsr */
                            if (iset[i] == '\0') { ++data.i586; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 'p':
                    switch(iset[i++])
                    {
                    case 'm':
                        switch(iset[i++])
                        {
                        case 'c': /* rdpmc */
                            if (iset[i] == '\0') { ++data.ppro; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                case 't':
                    switch(iset[i++])
                    {
                    case 's':
                        switch(iset[i++])
                        {
                        case 'c': /* rdtsc */
                            if (iset[i] == '\0') { ++data.i586; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'o':
                switch(iset[i++])
                {
                case 'u':
                    switch(iset[i++])
                    {
                    case 'n':
                        switch(iset[i++])
                        {
                        case 'd':
                            switch(iset[i++])
                            {
                            case 'p':
                                switch(iset[i++])
                                {
                                case 'd': /* roundpd */
                                    if (iset[i] == '\0') { ++data.sse4_1; }
                                    break;
                                case 's': /* roundps */
                                    if (iset[i] == '\0') { ++data.sse4_1; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            case 's':
                                switch(iset[i++])
                                {
                                case 'd': /* roundsd */
                                    if (iset[i] == '\0') { ++data.sse4_1; }
                                    break;
                                case 's': /* roundss */
                                    if (iset[i] == '\0') { ++data.sse4_1; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 's':
                switch(iset[i++])
                {
                case 'm': /* rsm */
                    if (iset[i] == '\0') { ++data.i586; }
                    break;
                case 'q':
                    switch(iset[i++])
                    {
                    case 'r':
                        switch(iset[i++])
                        {
                        case 't':
                            switch(iset[i++])
                            {
                            case 'p':
                                switch(iset[i++])
                                {
                                case 's': /* rsqrtps */
                                    if (iset[i] == '\0') { ++data.sse; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            case 's':
                                switch(iset[i++])
                                {
                                case 's': /* rsqrtss */
                                    if (iset[i] == '\0') { ++data.sse; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            default:
                ++data.unused;
                break;
            }
            break;
        case 's':
            switch(iset[i++])
            {
            case 'f':
                switch(iset[i++])
                {
                case 'e':
                    switch(iset[i++])
                    {
                    case 'n':
                        switch(iset[i++])
                        {
                        case 'c':
                            switch(iset[i++])
                            {
                            case 'e': /* sfence */
                                if (iset[i] == '\0') { ++data.sse;++data.tdnow2; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'h':
                switch(iset[i++])
                {
                case 'u':
                    switch(iset[i++])
                    {
                    case 'f':
                        switch(iset[i++])
                        {
                        case 'p':
                            switch(iset[i++])
                            {
                            case 'd': /* shufpd */
                                if (iset[i] == '\0') { ++data.sse2; }
                                break;
                            case 's': /* shufps */
                                if (iset[i] == '\0') { ++data.sse; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'q':
                switch(iset[i++])
                {
                case 'r':
                    switch(iset[i++])
                    {
                    case 't':
                        switch(iset[i++])
                        {
                        case 'p':
                            switch(iset[i++])
                            {
                            case 'd': /* sqrtpd */
                                if (iset[i] == '\0') { ++data.sse2; }
                                break;
                            case 's': /* sqrtps */
                                if (iset[i] == '\0') { ++data.sse; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        case 's':
                            switch(iset[i++])
                            {
                            case 'd': /* sqrtsd */
                                if (iset[i] == '\0') { ++data.sse2; }
                                break;
                            case 's': /* sqrtss */
                                if (iset[i] == '\0') { ++data.sse; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 't':
                switch(iset[i++])
                {
                case 'm':
                    switch(iset[i++])
                    {
                    case 'x':
                        switch(iset[i++])
                        {
                        case 'c':
                            switch(iset[i++])
                            {
                            case 's':
                                switch(iset[i++])
                                {
                                case 'r': /* stmxcsr */
                                    if (iset[i] == '\0') { ++data.sse; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'u':
                switch(iset[i++])
                {
                case 'b':
                    switch(iset[i++])
                    {
                    case 'p':
                        switch(iset[i++])
                        {
                        case 'd': /* subpd */
                            if (iset[i] == '\0') { ++data.sse2; }
                            break;
                        case 's': /* subps */
                            if (iset[i] == '\0') { ++data.sse; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    case 's':
                        switch(iset[i++])
                        {
                        case 'd': /* subsd */
                            if (iset[i] == '\0') { ++data.sse2; }
                            break;
                        case 's': /* subss */
                            if (iset[i] == '\0') { ++data.sse; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            default:
                ++data.unused;
                break;
            }
            break;
        case 'u':
            switch(iset[i++])
            {
            case 'c':
                switch(iset[i++])
                {
                case 'o':
                    switch(iset[i++])
                    {
                    case 'm':
                        switch(iset[i++])
                        {
                        case 'i':
                            switch(iset[i++])
                            {
                            case 's':
                                switch(iset[i++])
                                {
                                case 'd': /* ucomisd */
                                    if (iset[i] == '\0') { ++data.sse2; }
                                    break;
                                case 's': /* ucomiss */
                                    if (iset[i] == '\0') { ++data.sse; }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'd':
                switch(iset[i++])
                {
                case '2': /* ud2 */
                    if (iset[i] == '\0') { ++data.ppro; }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'n':
                switch(iset[i++])
                {
                case 'p':
                    switch(iset[i++])
                    {
                    case 'c':
                        switch(iset[i++])
                        {
                        case 'k':
                            switch(iset[i++])
                            {
                            case 'h':
                                switch(iset[i++])
                                {
                                case 'p':
                                    switch(iset[i++])
                                    {
                                    case 'd': /* unpckhpd */
                                        if (iset[i] == '\0') { ++data.sse2; }
                                        break;
                                    case 's': /* unpckhps */
                                        if (iset[i] == '\0') { ++data.sse; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            case 'l':
                                switch(iset[i++])
                                {
                                case 'p':
                                    switch(iset[i++])
                                    {
                                    case 'd': /* unpcklpd */
                                        if (iset[i] == '\0') { ++data.sse2; }
                                        break;
                                    case 's': /* unpcklps */
                                        if (iset[i] == '\0') { ++data.sse; }
                                        break;
                                    default:
                                        ++data.unused;
                                        break;
                                    }
                                    break;
                                default:
                                    ++data.unused;
                                    break;
                                }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            default:
                ++data.unused;
                break;
            }
            break;
        case 'w':
            switch(iset[i++])
            {
            case 'b':
                switch(iset[i++])
                {
                case 'i':
                    switch(iset[i++])
                    {
                    case 'n':
                        switch(iset[i++])
                        {
                        case 'v':
                            switch(iset[i++])
                            {
                            case 'd': /* wbinvd */
                                if (iset[i] == '\0') { ++data.i486; }
                                break;
                            default:
                                ++data.unused;
                                break;
                            }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'r':
                switch(iset[i++])
                {
                case 'm':
                    switch(iset[i++])
                    {
                    case 's':
                        switch(iset[i++])
                        {
                        case 'r': /* wrmsr */
                            if (iset[i] == '\0') { ++data.i586; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            default:
                ++data.unused;
                break;
            }
            break;
        case 'x':
            switch(iset[i++])
            {
            case 'a':
                switch(iset[i++])
                {
                case 'd':
                    switch(iset[i++])
                    {
                    case 'd': /* xadd */
                        if (iset[i] == '\0') { ++data.i486; }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            case 'o':
                switch(iset[i++])
                {
                case 'r':
                    switch(iset[i++])
                    {
                    case 'p':
                        switch(iset[i++])
                        {
                        case 'd': /* xorpd */
                            if (iset[i] == '\0') { ++data.sse2; }
                            break;
                        case 's': /* xorps */
                            if (iset[i] == '\0') { ++data.sse; }
                            break;
                        default:
                            ++data.unused;
                            break;
                        }
                        break;
                    default:
                        ++data.unused;
                        break;
                    }
                    break;
                default:
                    ++data.unused;
                    break;
                }
                break;
            default:
                ++data.unused;
                break;
            }
            break;
    default:
        ++data.unused;
        break;
    }
